# AWS Configuration
AWS_REGION=us-east-1
AWS_VECTOR_REGION=us-east-1
REGION=us-east-1

# S3 Buckets
BUCKET_NAME=your-bucket-name
VECTOR_BUCKET=your-vector-bucket
VECTOR_INDEX=document-embeddings

# DynamoDB Tables
CHATS_TABLE=chat_table
CHAT_HISTORY_TABLE=chatHistory_table
INDEX_AUDIT_TABLE=index_audit_table

# Model IDs (must be enabled in Bedrock)
EMBED_MODEL=amazon.titan-embed-text-v2:0
LLM_MODEL=amazon.nova-lite-v1:0
OCR_MODEL=amazon.nova-lite

# Configuration
EMB_DIM=1024
TOP_K=3
CHAT_HISTORY_WINDOW=2

# Optional: For legacy compatibility
S3_BUCKET_NAME=your-bucket-name
REST_ENDPOINT_URL=http://localhost:3000
